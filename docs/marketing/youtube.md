# YouTube / video marketing → see `marketing/youtube/`

All AgentHub demo-video marketing lives in **`marketing/youtube/`** (kept out of `docs/` because it holds
media). That folder's `README.md` is the source of truth:

- **Uploaded videos log** (URLs + which title/thumbnail each used) — complete the `{{TBD}}` URLs after each upload.
- **Edited demo cuts** (`videos/`): `agenthub-how-it-works-65s.mp4`, `claude-uploads-to-youtube-84s.mp4`; raw 4K originals referenced by path in `C:\Users\semma\Downloads\`.
- **Metadata packages** (`metadata-01-browser-mcp-hero.md`, `metadata-02-claude-uploads-to-youtube.md`) — title/description/tags/chapters, with a `{{CHROME_STORE_URL}}` to fill.
- **Assets**: `thumbnails/` (1280×720), `store-screenshots/` (1280×800 CWS), `screenshots/` (raw product captions).
- **How-to / next-time playbook**: modify, re-upload, change metadata, or script a new AgentHub demo.

Store-listing copy (separate from YouTube) stays in `docs/store-listing.md` + `docs/store-description*.txt`.
